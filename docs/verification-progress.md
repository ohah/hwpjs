# 프로젝트 문서 검증 현황

검증 대상은 Git이 추적하는 현재 프로젝트의 Markdown 문서입니다. `legacy/`와 `reference/`는 이전 구현·외부 참고 자료이므로 이 집계에서 제외하고, 별도로 인용할 때 해당 부분을 확인합니다. 현재 작업 트리에서 삭제 상태인 `lint-rules.md`는 사용자의 변경을 보존하며 `missing`으로 표시합니다. 존재하지 않는 파일을 임의로 복구하거나 검증 완료로 세지 않습니다.

`tools/docs-audit-status.mjs`가 추적 대상·현재 내용 해시가 일치하는 검증 완료 파일·미검증·변경 후 재검증 필요·작업 트리 누락·새 미추적 문서를 구분합니다. 실행 방법은 [개발·검증 명령](development-commands.md)이 소유합니다. 완료 게이트는 **목록/해시 관점의** 검사일 뿐입니다. 검증 완료 목록의 단일 출처는 `tools/docs-audit-reviewed.json`이며, 문서가 바뀌면 해당 항목은 자동으로 `stale`이 됩니다. 이 수치는 제품 구현률이 아닙니다.

파일을 완료 목록에 넣기 전에는 링크/실제 경로, 현재 코드와의 계약 일치, 명령의 실행 가능성과 적용 범위, 실파일·명세 근거 및 미지원 경계를 확인합니다. 링크 검사나 테스트 성공만으로 해당 파일을 완료 처리하지 않습니다. 전체 완료 판단에는 목록 게이트 외에 문서끼리의 모순, 누락된 요구 사항, 대표 실파일과 오류 경로를 다시 보는 최종 적대적 검증이 필요합니다.

기존 문서의 과거 테스트 기록은 이 목록에 자동 소급하지 않습니다. 이 목록의 비율은 새 기준에 따라 **현재 내용 전체를 다시 검증한 파일의 비율**이며, 이전 구현 진행률이나 과거 검증 작업량의 추정치가 아닙니다.

2026-09-27 기준 첫 완료 파일은 [HWP5 각주·미주 원문 표식 위치](hwp5-note-source-sites.md)입니다. 해당 주제의 공식 명세·제품 코드·원시 레코드 대조와 반례, 감사 테스트 결과는 그 문서에 둡니다. [프로젝트 구조](project-structure.md)는 HWPX XML 트리·section·WMF/EMF 범위 표현을 교정했으나 파일 전체의 검증 완료로 세지 않습니다. 새 현황 문서까지 포함해 현재 존재하는 프로젝트 Markdown 560개에서 일반 인라인 로컬 링크 2,518개의 대상 파일이 모두 존재했습니다. 작업 트리에서 삭제된 `lint-rules.md`는 이 링크 검사에서 제외됩니다. 참조형 링크·앵커·외부 URL·문서 내용의 옳음까지 증명하는 검사는 아닙니다.

진입점 [AGENTS.md](../AGENTS.md)는 링크 대상과 인덱스 전용 책임을, [커밋 규칙](../commit-rules.md)은 프로젝트 규칙·실제 빌드 명령·레거시 규칙 경로와의 일치를 확인해 완료 목록에 넣었습니다. [README](../README.md)도 빌드·공개 JS API·두 실행 예제를 확인했습니다. Node에서는 추적 HWP fixture의 `/BodyText/Section0`이 `Buffer` 402바이트였고, 실제 Chromium에서는 HTTP로 받은 WASM/fixture의 strict 읽기 후 `/FileHeader`가 값 256개의 배열이었습니다. README의 `sample.hwp`는 사용자가 제공할 입력 예시이므로 검사에는 같은 저장소의 추적 fixture를 사용했습니다.

`docs/project-rules.md`는 실제 모듈·레거시 명세 경로, 진입점/상세 문서의 소유권, 현 커밋·검증 명령과 충돌하지 않는지 확인해 완료 목록에 넣었습니다. 구현 원칙은 정책이지 모든 HWP/HWPX 기능이 이를 이미 충족했다는 선언으로 읽지 않습니다.

HWPX 주석의 `hwpx-note-sites.md`·`hwpx-note-bodies.md`·`hwpx-note-number-links.md`·`hwpx-note-text.md`는 공식 한컴 모델/구조 설명, 현 Zig 소유권·수명 경계, 세 빌드 모드의 주석 계열 27개 테스트, 네 독립 오라클의 일반/최적화 변이 검사, 476개 허용 실파일의 파일별 대조와 단독/known 경로를 확인해 완료 목록에 넣었습니다. known-inspections도 8개 shard를 **별도 프로세스**로 재실행해 각각 통과했으며 합계는 허용 476개·ZIP 거부 6개·암호화 2개·section 544개였습니다. 이 공통 실측의 제품 코드 기준은 `2570e219`, 로컬 rhwp 샘플 리비전은 `e8800c8de`이고 샘플 변경은 없었습니다. 각 주제의 현재 재현값과 미지원 범위는 해당 문서가 소유합니다. `docs/development-commands.md`는 1차 정적 조사에서 추출한 85개 경로가 존재하고 인용 필터 156개 중 155개가 테스트 이름과 대응했으며 나머지 하나는 명시적 `<이름>` 자리표시자였습니다. 이 조사는 모든 명령의 실행 성공 검증이 아니므로 명령 문서 전체는 아직 완료로 세지 않습니다.

HWPX 번호 컨트롤과 단 설정 문서도 한컴 고정 버전 모델의 필드·열거형, 현재 검사기의 소유 경계, 세 모드의 집중 테스트, 독립 오라클 자체검사 및 허용 476개 파일별 결과, 실파일 단독/known 경로를 다시 확인해 완료 목록에 넣었습니다. 기준 제품 코드 `752ca0d2`는 이전 known-inspections 8개 shard가 통과한 코드 `2570e219`에서 문서만 변경된 상태이고, 로컬 rhwp 샘플은 `e8800c8de`입니다. 이 재사용 증거는 보고서 조립 경로에 한정하며 번호 계산·단 조판·저장 기능을 입증하지 않습니다.

HWPX 필드 마커와 인라인 문자열 컨트롤 문서도 공식 필드·자식 모델, 현재 코드의 namespace·링크·직접 텍스트 경계, 세 모드의 집중 테스트, 독립 오라클 자체검사와 476개 허용 파일의 파일별 결과, 양성 실파일의 단독/known 경로를 확인해 완료 목록에 넣었습니다. 필드 마커의 모델 밖 속성 11개는 별도 ZIP/XML 집계로 모두 `fieldBegin/@metaTag`임을 확인했습니다. 이 묶음의 제품 코드 기준은 `855b891c`이며 이전 known-inspections 8개 shard의 코드 기준 `2570e219` 이후 제품 코드는 변경되지 않았습니다. 기능별 미지원 범위는 각 주제 문서가 소유합니다.

HWPX section `metaTag`와 파라미터 목록 문서도 공식 모델·현재 선택/소유 경계·세 모드 집중 테스트·독립 오라클의 자체검사와 허용 476개 파일별 대조·실파일 known 연결을 확인해 완료 목록에 넣었습니다. `metaTag` 양성 네 파일의 부모·개별 텍스트 길이와 파라미터 목록의 양성 파일 수·스칼라 바이트·자식 종류별 수치도 별도 XML 집계로 재확인했습니다. 기준 제품 코드 `23831fd0`는 이전 known-inspections의 `2570e219` 이후 문서만 바뀐 상태이며, 로컬 rhwp 샘플 리비전은 `e8800c8de`입니다. 값의 제품 의미·렌더링·저장 지원까지 완료했다는 뜻은 아닙니다.

HWPX 수식 원문·도형 자식·`shapeComment`·caption의 네 문서도 한컴 고정 버전 모델, 현재 수식/도형/공통 caption 책임, 세 모드 집중 테스트, 독립 수식 오라클의 변이·476개 파일별 대조, 주석·caption 합성 고정 해시를 재확인해 완료 목록에 넣었습니다. 별도 ZIP/XML 인벤토리로 도형 자식 종류·속성값 총계와 주석 길이·속성·하위 요소 분포를 확인했습니다. corpus의 수식 caption은 0개여서 이 경로의 실파일 값 검증으로 세지 않습니다. 기준 제품 코드 `b085d347`는 기존 known-inspections 8개 shard의 `2570e219` 이후 문서만 바뀐 상태입니다. 수식 문법·조판·편집·저장은 미지원 경계로 유지합니다.

ParaListType 속성과 표 shape 문서도 고정 버전 공식 필드·현재 호출자 연결·공유 판독 코드·합성 반례, 마스터페이지/표 독립 ZIP/XML 오라클의 현재 실파일 집계와 기존 known-inspections 비교 범위를 확인해 완료 목록에 넣었습니다. ParaList 문서의 오래된 호출자/후속 문구는 각주·미주 및 표·수식 caption 연결을 포함하도록 교정했고, 마스터페이지 수치를 다른 호출자 경로로 일반화하지 않았습니다. 제품 코드 기준 `905c870b`는 기존 8개 shard 실행 기준 `2570e219` 이후 문서만 바뀐 상태입니다. 실제 레이아웃·편집·저장은 여전히 범위 밖입니다.

표 셀의 직접 `subList`, 표 자체 속성, 표 안쪽 여백·셀 구역의 세 문서도 고정 버전 모델·현재 소유 코드·세 모드 집중 테스트·독립 표 오라클의 자체 반례와 476개 허용 실파일 집계를 대조해 완료 목록에 넣었습니다. 셀별 목록 누락·중복, 표 ID 0의 미해결 참조, 여백의 음수·상위 비트 여부를 총계와 분리해 확인했습니다. 기존 known-inspections 8개 shard는 제품 코드가 변경되지 않아 이전 결과만 재사용했고 이번 묶음에서 재실행하지 않았습니다. 셀 본문 의미·표 레이아웃·편집·저장은 완료 범위가 아닙니다.

표 격자·셀 필드·행/셀 직접 자식 topology 세 문서도 공식 모델, 현재 소유 코드, 세 모드 집중 테스트와 독립 표 오라클의 476개 허용 실파일 수치를 다시 대조해 완료 목록에 넣었습니다. 격자 진단 0건, 셀 음수/상위 비트 여백, ID 참조 범위, 두 자식 순서의 정확한 분포를 확인했습니다. 격자 문서의 분기·caption 표현을 현재 모듈 경계에 맞게 좁히고, topology 문서의 미재검증 버전별 파일 수 단정은 제거했습니다. known-inspections 8개 shard는 제품 코드 불변을 확인한 이전 결과를 재사용했고 이번 묶음에서 다시 실행하지 않았습니다. 이 결과는 구조·원값 관측의 문서 검증이지 표 레이아웃·편집·저장 완성이 아닙니다.

마스터페이지 표 격자와 조건부 선택 표 격자 문서도 현재 공통 격자/분기 정책·파트 예산 경계, 세 모드 합성 반례, 공유 선택 정책 회귀 및 독립 표 오라클의 476개 허용 실파일 집계를 재확인해 완료 목록에 넣었습니다. 마스터페이지 61개에서 표 48개·셀 154개를 관측했지만 section·마스터페이지의 `switch` 후손 표는 각각 0개여서 선택 분기 표의 실파일 양성 검증으로 세지 않습니다. 기존 known-inspections 8개 shard는 제품 코드 불변을 확인한 이전 결과만 재사용했고 이번 묶음에서 다시 실행하지 않았습니다. 조판·편집·저장과 JS/WASM 공개 API는 완료 범위가 아닙니다.

조건부 참조 선택·run 내부 `switch` 구조·선택 section 텍스트의 세 문서도 한컴 고정 리비전 선택 규칙, 현재 Zig의 공유 정책과 원문/선택 책임, 세 모드 집중 테스트, 독립 구조·텍스트 오라클의 허용 476개 파일 집계를 대조해 완료 목록에 넣었습니다. 직접 `switch` 93개의 chart/OLE 구조와 한 파일의 분기별 텍스트 제거량 2/2/2개·173바이트를 구분했습니다. 기존 known-inspections 8개 shard는 제품 코드 불변을 확인한 이전 결과를 재사용했고 이번 묶음에서 다시 실행하지 않았습니다. 렌더링·편집·저장과 전체 조건부 문서 모델은 완료 범위가 아닙니다.

선택 section·마스터페이지 서식 참조와 section 텍스트 스냅샷의 세 문서도 현재 공유 선택/ID 판독·소유/한도 경계, 세 모드 집중 테스트, 독립 XML 분기 집계와 스냅샷의 원문·기본 선택·차트 선택 476개 파일별 내용/순서 해시를 대조해 완료 목록에 넣었습니다. 마스터페이지 `switch`는 실파일 0개라 해당 선택 경로의 양성 증거는 합성 반례에 한정합니다. `hp:t` 후손 동명 `run`에 대한 텍스트/서식 스캐너의 범위 차이를 발견해 두 서식 문서에 명시했으며, 실파일 문단·run 수 일치를 일반 동치로 확대하지 않습니다. 기존 known-inspections 8개 shard는 제품 코드가 불변이라 이전 결과를 재사용했고 이번 묶음에서 다시 실행하지 않았습니다. 화면 서식·조판·편집·저장은 완료 범위가 아닙니다.

section 서식 참조·마스터페이지 서식 참조·마스터페이지 파트의 세 문서는 현 소유 코드와 한컴 고정 리비전 모델의 필드·참조 경계, Debug·ReleaseSafe·ReleaseFast 집중 테스트, 독립 ZIP/XML 오라클의 허용 476개 파일 집계 및 section 제품 corpus 조사 결과를 다시 대조해 완료 목록에 넣었습니다. 마스터페이지는 61파트·61 section 참조가 모두 연결됐고, 서식 참조는 394개 문단·521개 run에서 모두 해결됐습니다. section에서는 스타일 대상 그룹이 없는 문서 21개에서 참조 258건을 확인해 성공으로 바꾸지 않았습니다. 마스터페이지 문서에 남은 오래된 ‘서식 ID 연결은 후속’ 문구를 별도 검사기 소유로 바로잡았습니다. 기존 known-inspections 8개 shard는 제품 코드가 불변이라 이전 결과만 재사용하고 이번 묶음에서는 재실행하지 않았습니다. 표시·조판·편집·저장은 검증 범위가 아닙니다.

header·spine 구조, header 리소스 ID 색인, header 내부 참조의 세 문서는 현재 코드의 소유·namespace·ID·한도 경계, 한컴 공식 구조 설명과 고정 리비전 필드, 세 모드 집중 테스트, 두 추적 실파일의 header 필드·ID 독립 ZIP/XML 조회 및 각각 단독 실행한 476개 실파일 제품 조사를 대조해 완료 목록에 넣었습니다. 구조 조사에서는 section 544개와 `secCnt` 불일치 1건, 리소스 조사에서는 일곱 그룹의 부재·선언 수치, 내부 참조 조사에서는 여섯 연결의 해결·미해결 수치와 minor 분포를 재확인했습니다. 처음 지정한 `HWPX header references` 필터는 테스트가 없어서 증거로 세지 않고 실제 `HWPX header links` 필터로 다시 실행했습니다. 과거 전체 테스트·변이 검사 수치는 당시 기록으로 분리했으며 이번 묶음에서 전체 audit나 변이 검사를 다시 실행하지 않았습니다. 메모 리소스의 corpus 수치, 전체 스키마·표시·저장 지원을 이 세 문서의 완료로 주장하지 않습니다.

언어별 글꼴 ID·번호/글머리표 내부 참조·구역 정의 속성·구역 정의 ID 참조의 네 문서는 현재 공유 ID/원값 코드와 한컴 고정 리비전 모델, 세 모드 집중/표본 테스트, 글꼴·목록 단독 476개 실파일 조사, 구역 정의의 독립 ZIP/XML 조사기 두 개와 별도 ReleaseFast 제품 `inspectKnown` 8개 shard를 대조해 완료 목록에 넣었습니다. 글꼴의 여섯 언어 목록 부재, 글머리표 5건의 대상 그룹 부재, 구역 번호 테이블 부재 21건 및 원시 ID 0은 임의 기본값이나 오류로 바꾸지 않았습니다. 구역 정의 조사기 두 개의 자체 반례가 Python `assert`에 의존해 `-O`에서 무력화되던 결함을 고치고, 고장 주입도 `-O`에서 잡는지 확인했습니다. 실파일 연결은 이 네 계약의 관측 범위에 한정하며 폰트 표시·번호 조판·구역 레이아웃·편집·저장은 아직 증명하지 않습니다. 이번 묶음에서 전체 audit와 과거 변이 검사는 다시 실행하지 않았습니다.

구역 쪽 설정·직접 설정·쪽 테두리 원값·테두리 ID 참조의 네 문서도 현재 소유 코드, 한컴 고정 리비전 필드·열거형, 세 모드 집중/실파일 테스트와 각각의 독립 ZIP/XML corpus 집계를 다시 대조해 완료 목록에 넣었습니다. 쪽 설정 555개, 직접 설정의 `strikeContinue="0"` 21개, 쪽 테두리 1,615개 중 ID 0의 대상 누락 30개를 원문대로 유지합니다. 세 조사기의 Python `assert` 자체 반례를 `-O`에서도 작동하게 고쳤고, 쪽 설정의 잘못된 숫자 어휘·u32 초과 수용 및 쪽 설정/직접 설정의 손상 파일 부분 집계 가능성도 없앴습니다. 조사기 일반·최적화 전체 결과가 일치했고 고장 주입 반례도 실패했습니다. 직전 묶음에서 제품 코드가 같은 상태로 통과한 known 8개 shard를 재사용했고 이번에는 다시 실행하지 않았습니다. 쪽 적용 우선순위·레이아웃·편집·저장은 이 검증의 완료 범위가 아닙니다.

구역 각주·미주 모양과 프레젠테이션 원값 문서도 현재 Zig 필드/스캐너, 고정 리비전 한컴 모델, 세 모드 집중/실파일 테스트, 독립 ZIP/XML 조사기의 484개 후보·476개 수용 파일 분포를 대조해 완료 목록에 넣었습니다. note 모양의 누락 문자 속성·비정형 폭/색상/배치와 프레젠테이션의 빈 `soundIDRef`를 그대로 기록합니다. 두 조사기의 Python `assert` 반례를 `-O`에서도 작동하도록 수정하고 전체 shard 출력 일치 및 고장 주입 실패를 확인했습니다. 이번 묶음에서 known 8개 shard와 전체 audit는 재실행하지 않았으며 과거 통과 결과를 현재 제품 전체 검증으로 확대하지 않습니다. 실제 조판·재생·편집·저장은 여전히 범위 밖입니다.

section 원문 트리·직접 콘텐츠·텍스트 이벤트의 세 문서는 현재 공유 인덱스/재생기와 텍스트 스캐너, Debug·ReleaseSafe·ReleaseFast 집중 테스트, 독립 ZIP/XML/Expat 조사, 제품 트리 8개 별도 ReleaseFast shard 및 제품 텍스트 전체 조사로 다시 검증했습니다. 트리 문서의 비접두 속성 일괄 조회 누락을 교정하고, 한컴 2021 namespace 스키마 예를 현재 2011 지원 범위와 구분했습니다. 독립 조사기의 `assert` 자체 반례와 늦은 ZIP 오류 시 부분 집계를 고치고 일반·`-O` 전체 corpus 일치, 고장 주입 및 기존 수용 문서 보존을 확인했습니다. corpus의 476개 수용·544개 section·2,174,716개 요소와 텍스트 230,677개를 다시 대조했지만, 개별 XML 콜백 분할·화면 결과·편집·저장까지 동치라고 주장하지 않습니다. 이번 묶음에서 전체 audit는 재실행하지 않았습니다.

마이그레이션 이력과 프로젝트 구조 인덱스는 현재 경로·워크플로·빌드 정의·공개 JS 경계를 확인해 완료 목록에 넣었습니다. 레거시 Cargo metadata와 `hwp-core` 테스트가 통과했고 Bun 웹 개발 서버의 시작 후 종료를 확인했습니다. 이동 커밋의 내용 동일 `R100` 변경 1,541개는 확인했지만 당시 비추적 파일을 포함한 1,652개 전체의 원본 해시 대조는 현재 Git 이력으로 재현하지 못하므로 이력 기록으로만 남겼습니다. 현재 프로젝트 문서의 로컬 인라인 링크 2,520개 대상 검사, ReleaseSafe 제품 빌드, JS/WASM 비교 47개는 통과했습니다. 전체 Debug Zig 테스트는 약 12분 뒤 `inspectKnown` 할당 실패 반복 검사 중 의도적으로 중단해 통과로 세지 않으며, 각 상세 문서의 기능 검증이나 최종 적대적 검증을 이 인덱스 검증으로 대체하지 않습니다.

HWP5 모듈 계약 인덱스는 현재 상위 계약 분류와 하위 주제 링크의 대상, 실제 HWP5 chart·OLE 모듈 경계를 확인해 완료 목록에 넣었습니다. 인덱스 검증은 링크된 각 계약 문서의 기능·명세·실파일 검증을 대신하지 않습니다. 아키텍처 문서는 현재 구현된 공통 fillBrush·그림 payload·조건부 선택 계층과 CFB 전용 JS API 범위에 맞게 오래된 설명을 고쳤지만, 수백 개의 세부 계약 전체를 재대조하지 않았으므로 아직 완료 목록에 넣지 않았습니다. 현재 로컬 인라인 링크 2,523개는 모두 존재했으며, 링크 대상 존재는 내용의 정확성 증거가 아닙니다.

HWP5 문서 조립·컨테이너·별도 스트림 상위 계약은 현재 decoded 문서/strict CFB 연결, 정확한 BinData·미리보기·summary·Scripts·XMLTemplate·DocHistory 선택과 보고서 수명 경계를 소스에서 대조하고 ReleaseSafe 6개·ReleaseFast 3개 집중 테스트를 확인해 완료 목록에 넣었습니다. 실파일 corpus와 전체 audit는 이번 묶음에서 재실행하지 않았고, 링크된 하위 주제 문서의 필드별 명세·지원 완료를 이 상위 계약에 소급하지 않습니다.

HWP5 내부 XML 검증 연결 문서는 로컬 명세의 XMLTemplate·DocHistory 대상, 현재 공유 XML 예산·선택·수명, 세 빌드 모드 집중 테스트와 독립 이력 조사기 안전 테스트를 대조해 완료 목록에 넣었습니다. 실파일 이력 XML 5개의 payload 13,291,898바이트와 decoded 13,292,279바이트를 다시 확인했고 ReleaseSafe HWP5 WASM 감사는 10/10 단계·전체 8,905,855 checks로 통과했습니다. 문서의 과거 3,082,967 checks·정상/거부 106/122건은 당시 실행 기록으로 분리했으며 이번에 전체 Debug·ReleaseFast audit나 XMLTemplate 양성 실파일을 검증한 것으로 세지 않습니다.
