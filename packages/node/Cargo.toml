[package]
name = "hwp-node"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Node.js bindings for HWP file reading using NAPI-RS"
repository = "https://github.com/ohah/hwpjs"

[lib]
name = "hwp_node"
crate-type = ["cdylib"]

[dependencies]
hwp-core = { path = "../../crates/hwp-core" }
# NAPI-RS dependencies will be added when initializing with NAPI-RS CLI

