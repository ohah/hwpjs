import assert from 'node:assert/strict';
import {writeUnsigned} from './icc-wide-fraction-wire.mjs';
import {rationalPowerOrder,exponent,cmp} from './icc-power-reference.mjs';
export function ordinateOrderInput(value,target,precision=512){const out=Buffer.alloc(104);out.writeUInt32BE(precision);writeUnsigned(out,4,target[0],16);writeUnsigned(out,20,target[1],16);if(value.rational){writeUnsigned(out,40,value.rational[0]);writeUnsigned(out,72,value.rational[1]);}else{out.writeUInt32BE(1,36);writeUnsigned(out,40,BigInt.asUintN(128,value.base[0]),16);writeUnsigned(out,56,value.base[1],16);out.writeInt32BE(value.g,72);out.writeInt32BE(value.offset,76);}return out;}
export function ordinateOrderEdges(call){let comparisons=0,rejected=0,undecided=0;
 function check(value,target,precision=512){const bytes=ordinateOrderInput(value,target,precision);let error;if(target[1]===0n||target[0]>target[1]||value.rational&&(value.rational[1]===0n||value.rational[0]>value.rational[1]))error=/InvalidIccCurveCoordinate/;else if(!value.rational){if(value.base[1]===0n)error=/InvalidIccPowerCoordinate/;else if(value.base[0]===0n&&value.g<=0||value.base[0]<0n&&exponent(value.g)[1]>1n)error=/UndefinedIccCurvePower/;}if(error){assert.throws(()=>call(208,bytes),error);rejected++;return;}const expected=value.rational?cmp(value.rational,target):rationalPowerOrder(value.base,value.g,[target[0]*65536n-BigInt(value.offset)*target[1],target[1]*65536n]);const out=call(208,bytes);assert.equal(out.length,4);assert.equal(out.readInt32LE(),expected);comparisons++;}
 for(const g of [-131072,-65536,-32768,0,32768,65536,131072])for(const a of [-3n,0n,1n,3n])for(const b of [1n,3n,7n])for(const offset of [-2147483648,-32768,0,16384,2147483647])for(const target of [[0n,1n],[1n,3n],[1n,2n],[1n,1n]])check({base:[a,b],g,offset},target);
 const max=(1n<<128n)-1n;
 for(const precision of [128,256,512,1024]){for(const offset of [-2147483648,-32768,16384,2147483647])check({base:[1n,1n],g:0,offset},[max/3n,2n*(max/3n)],precision);for(const rational of [[0n,1n],[(1n<<255n)-1n,(1n<<256n)-1n],[1n,1n]])check({rational},[1n,2n],precision);}
 const root={base:[1n,2n],g:32768,offset:0},hi=[11749380235262596085n,16616132878186749607n];assert.equal(call(208,ordinateOrderInput(root,hi,128)).readInt32LE(),2);undecided++;check(root,hi,512);
 const good=ordinateOrderInput(root,[1n,2n]);for(let len=0;len<good.length;len++){assert.throws(()=>call(208,good.subarray(0,len)),/InvalidProbeInput/);rejected++;}for(const [b,l]of [[good,103],[Buffer.concat([good,Buffer.alloc(1)]),undefined]]){assert.throws(()=>call(208,b,l),/InvalidProbeInput|LimitExceeded/);rejected++;}
 for(const [position,value,error]of [[0,64,/InvalidIccComparisonPrecision/],[36,2,/InvalidProbeInput/],[80,1,/InvalidProbeInput/]]){const b=Buffer.from(good);b.writeUInt32BE(value,position);assert.throws(()=>call(208,b),error);rejected++;}
 check(root,[0n,0n]);check(root,[2n,1n]);check({...root,base:[1n,0n]},[0n,1n]);check({rational:[2n,1n]},[0n,1n]);check({rational:[0n,0n]},[0n,1n]);check(root,[1n,2n]);return {comparisons,rejected,undecided};
}
