/// 테이블 렌더링 상수 / Table rendering constants
/// SVG viewBox 패딩 (mm). viewBox 크기 = content_size + 2*padding. 콘텐츠 크기는 파싱값(content_size)만 사용.
/// SVG viewBox padding (mm). viewBox size = content_size + 2*padding. Content size from parsed data only.
pub(crate) const SVG_PADDING_MM: f64 = 2.5;
pub(crate) const BORDER_OFFSET_MM: f64 = 0.06;
